exploding gradient could occur due to high learning rate/using relu multiple times leads to exploding gradient.

gradient clipping:-we clip our gradients at a maximum value so that it cant increase over it.
